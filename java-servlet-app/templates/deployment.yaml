apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "java-servlet-app.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "java-servlet-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "java-servlet-app.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
